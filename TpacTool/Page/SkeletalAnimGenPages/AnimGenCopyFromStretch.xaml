<Page x:Class="TpacTool.SkeletalAnimGenPages.AnimGenCopyFromStretch"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TpacTool.SkeletalAnimGenPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AnimGenCopyFromStretch">
    <Page.DataContext>
        <Binding Path="SkeletalAnimGenCopyFrom" Source="{StaticResource Locator}"/>
    </Page.DataContext>
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}"/>
        </Grid.Background>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid>
                    <Label Content="Target Base Anim"/>
                    <TextBox Text="{Binding BaseTargetAnimName, Mode=TwoWay}" IsReadOnly="False" Height="22" Margin="168,0,0,0" VerticalAlignment="Center" BorderBrush="#FFABADB3"/>
                </Grid>
                <Grid>
                    <Label Content="Target Base Anim Bone Index"/>
                    <TextBox Text="{Binding BaseTargetIndex, Mode=TwoWay}" IsReadOnly="False" Height="22" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Target Base Anim Frame"/>
                    <TextBox Text="{Binding BaseTargetFrame, Mode=TwoWay}" IsReadOnly="False" Height="22" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Copied Anim Name"/>
                    <TextBox Text="{Binding CopiedAnimName, Mode=TwoWay}" IsReadOnly="False" Height="22" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Copied Bone Index"/>
                    <TextBox Text="{Binding CopiedBoneIndex, Mode=TwoWay}" IsReadOnly="False" Height="22" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Time Offset"/>
                    <TextBox Text="{Binding TimeOffset, Mode=TwoWay}" IsReadOnly="False" Height="22" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Add From Base"/>
                    <CheckBox IsChecked="{Binding AddFromBase, Mode=TwoWay}" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Ignore Target Zero"/>
                    <CheckBox IsChecked="{Binding IgnoreThisZeroFrame, Mode=TwoWay}" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
                <Grid>
                    <Label Content="Ignore Source Zero"/>
                    <CheckBox IsChecked="{Binding IgnoreSourceZeroFrame, Mode=TwoWay}" Margin="168,0,0,0" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
